
<title> Hello! I'm the games page! </title>
<p>
    Im a paragraph -- games
</p>

<%= button_to "New Game", {:controller => "game", :action => "create", :method=>:PUT, :remote => :true} %>


<p>Moves Remaining: <%= @game.moves_remaining %></p>

<% if @game.state == 1 %>
 	<p>You won!</p>
<% elsif @game.state == -1 %>
 	<p>You Lost</p>
<% else %>

<% end %>

<div class="board">
	<% @game.board.game_board.each do |x| %>
	   	<% x.each do |y| %>
	    	<div class="board_btn">
	    	
	    		<%= button_to y, {:controller => "game", :action => "update", :flood_color => y}, :method=>:PUT, :remote => :true %>
	    	</div>
	    <% end %>

	<% end %>
</div>

<script>
function flood(color) {

}
</script>

<%= form_tag game_path, remote: true do %>
  <%= hidden_field_tag :color, "color" %>
<% end %>

